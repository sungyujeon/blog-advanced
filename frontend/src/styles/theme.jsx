import { css } from 'styled-components';

const theme = {
  colors: {
    black: '#000000',
    white: '#ffffff',
  },

  font: {
    bold: css`
      font-style: normal;
      font-weight: bold;
    `,
  }
};

export default theme;